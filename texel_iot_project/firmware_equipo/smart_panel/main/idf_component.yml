## IDF Component Manager Manifest File
dependencies:
  idf:
    version: ">=5.0.0"

  # --- LO NUEVO QUE NECESITAS ---
  # Esto descarga el driver moderno de tu placa (compatible con S3)
  espressif/esp32_s3_lcd_ev_board: "^3.0"
  
  # Esto fuerza a usar LVGL 8.3 (la versión que usa EEZ Studio)
  lvgl/lvgl: "^8.3"

  # --- TUS DEPENDENCIAS ORIGINALES (Mantenlas) ---
  espressif/zlib: ^1
  espressif/esp-sr: "1.3.4"
  espressif/esp-dsp: "==1.5.2"

  espressif/esp_wifi_remote: "^1.0"